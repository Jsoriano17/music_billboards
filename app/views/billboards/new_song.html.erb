<h1>New Billboard page</h1>
<h1><%= @billboard.name %> </h1>

	<% @songs.each do |song| %>
	  <li>
	    <%= link_to song.name, add_billboard_song_path(@billboard, song), method: :post %>
	  </li>
	  <hr />
	<% end %>


<%= link_to 'Back', billboards_path%>