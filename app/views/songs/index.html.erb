<h1><%= "#{@artist.name}"%></h1>
<% @songs.each do |song| %>
  <h2><%= song.name %></h2>
  <%= link_to 'Song Info', artist_song_path(@artist, song) %>
   <%= link_to 'Edit Song', edit_artist_song_path(@artist, song) %>
    <%= link_to 'remove Song', [@artist, song], method: :delete %>

  
<% end %>

<h1>Create New Item</h1>
  <%= link_to 'Create New Song', new_artist_song_path %>
  <br>
   <br>
    <%= link_to 'Back', artists_path %>